{
    "version": "2.0.0",
    "tasks": [
        {
            "type": "shell",
            "label": "Activate Virtual Environment",
            "command": "${workspaceFolder}\\venv\\Scripts\\activate",
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "problemMatcher": []
        }
    ]
}